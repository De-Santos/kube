apiVersion: v1
kind: Service
metadata:
  name: clickhouse
  namespace: clickhouse
spec:
  type: ClusterIP
  selector:
    app: clickhouse
  ports:
  - port: 8123
    targetPort: 8123
    protocol: TCP
    name: http
  - port: 8200
    targetPort: 8200
    protocol: TCP
    name: native
